/**
 * lucide-react shim for React Native
 * Re-exports all icons from lucide-react-native so existing
 * `import { ... } from 'lucide-react'` calls work without changes.
 */
export * from "lucide-react-native";
